# render.yaml
services:
  - type: web
    name: discord-music-bot
    env: python
    plan: free
    region: oregon

    buildCommand: |
      pip install -U pip
      pip install -r requirements.txt

    startCommand: |
      python bot_local.py
